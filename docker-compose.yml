version: "2"
services:
  front:
    build: ./myApp/
    ports:
      - "3000:3000"
  local:
    build: ./local/
    ports:
      - "4242:4242"
  server:
    build: ./local/
    ports:
        - "5000:5000"
